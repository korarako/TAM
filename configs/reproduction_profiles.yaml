schema_version: 2
notes:
  - Profiles reproduce corrected v0.2 training settings; accelerator kernels need not be bitwise deterministic.
  - Reference/sample arrays and checkpoints are external to the source release.
  - Exact frozen configs and scripts under results/corrected take precedence over this convenience index.

profiles:
  mb2d_reference_v2_seed0:
    status: corrected_exact
    problem: mb2d
    model: mlp
    data_dir: data/mb2d_reference_v2/train
    fm: {beta_grid: [0.25, 0.5, 0.75, 1.0, 1.5], steps: 30000, batch_size: 512, lr: 0.001, lr_min: 0.00001, hidden_dim: 512, n_layers: 3, t_embed_dim: 16, beta_embed_dim: 8, prior_scale: 1.0, grad_clip: 1.0, seed: 0}
    am: {beta0: 1.0, beta1: 1.2, steps: 10000, batch_size: 512, lr: 0.00002, lr_min: 0.0000002, sde_steps: 40, loss_steps: 20, keep_last_steps: 10, max_sigma: 50.0, energy_grad_scale: 1.0, grad_clip: 1.0, am_parameterization: target-refinement, seed: 0}
    sampling: {beta: 1.2, num_samples: 100000, ode_steps: 300, method: euler, prior_scale: 1.0}

  dw4_reference_v2_seed2:
    status: corrected_exact
    problem: dw4
    model: egnn
    data_dir: data/dw4_reference_v2/train
    fm: {beta_grid: [0.8, 1.2], steps: 100000, batch_size: 512, lr: 0.0003, lr_min: 0.0003, hidden_dim: 128, n_layers: 5, t_embed_dim: 16, beta_embed_dim: 8, prior_scale: 1.0, grad_clip: 1.0, seed: 0}
    am: {beta0: 0.8, beta1: 1.0, steps: 1000, batch_size: 512, lr: 0.0000005, lr_min: 0.000000005, sde_steps: 40, loss_steps: 20, keep_last_steps: 10, max_sigma: 50.0, energy_grad_scale: 1.0, grad_clip: 1.0, am_parameterization: target-refinement, seed: 2}
    sampling: {beta: 1.0, num_samples: 100000, ode_steps: 150, method: euler, prior_scale: 1.0}

  lj13_reference_v2_seed2:
    status: corrected_exact
    problem: lj13
    model: painn
    data_dir: data/lj13_reference_v2/train
    fm: {beta_grid: [0.8, 1.2], steps: 100000, batch_size: 64, lr: 0.0003, lr_min: 0.0003, hidden_dim: 128, n_layers: 5, t_embed_dim: 16, beta_embed_dim: 8, prior_scale: 1.0, grad_clip: 1.0, seed: 0}
    am: {beta0: 0.8, beta1: 1.0, steps: 10000, batch_size: 512, lr: 0.00002, lr_min: 0.0000002, sde_steps: 40, loss_steps: 20, keep_last_steps: 10, max_sigma: 50.0, energy_grad_scale: 1.0, grad_clip: 1.0, am_parameterization: target-refinement, seed: 2}
    sampling: {beta: 1.0, num_samples: 100000, ode_steps: 150, method: euler, prior_scale: 1.0}
